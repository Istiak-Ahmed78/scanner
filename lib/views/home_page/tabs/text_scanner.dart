import 'package:flutter/cupertino.dart';

class TextScannerTab extends StatelessWidget {
  const TextScannerTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Text Scanner page'),
    );
  }
}
